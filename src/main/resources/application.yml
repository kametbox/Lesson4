spring:
  datasource:
    password: postgres
    username: postgres
    url: jdbc:postgresql://localhost:5432/lesson_3
  jpa:
    properties:
      hibernate:
        show_sql: true

    hibernate:
      ddl-auto: create