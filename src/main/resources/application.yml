spring:
  datasource:
    password: postgres
    username: postgres
    url: jdbc:postgresql://localhost:5432/lesson_4
  jpa:
    properties:
      hibernate:
        show_sql: true

    hibernate:
      ddl-auto: update